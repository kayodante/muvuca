# Dependencies
node_modules/
.pnpm-store/

# Next.js build output
.next/
out/
tsconfig.tsbuildinfo
*.tsbuildinfo
# Gerado pelo Next: `next dev` aponta para .next/dev/types, build/typegen
# para .next/types. O typecheck roda `next typegen` antes do tsc.
next-env.d.ts

# Env files — .env.example is the one exception, it's meant to be versioned.
.env*
!.env.example

# Supabase local stack
supabase/.branches/
supabase/.temp/
supabase/.env

# Editor/OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
pnpm-debug.log*

# Test/build output
coverage/
playwright-report/
test-results/

# Local Git worktrees
.worktrees/
